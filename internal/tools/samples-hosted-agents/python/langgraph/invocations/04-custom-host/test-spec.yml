version: 1

sample:
  owner: aochengwang
  experiences: [azd, vscode]

tests:
  - name: custom-host-smoke
    turns:
      - input: Reply with exactly LOCATION-AWARE-READY.
        assertions:
          - source: raw
            type: contains
            value: LOCATION-AWARE-READY
